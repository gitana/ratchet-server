exports.init = function(registry)
{
	// add ourselves to the registry
	registry.registerGadget("dashboard", {

    	read: function(store, config, req, res)
    	{
        	// TODO
        	var a = 3;
    	}

	});
};
